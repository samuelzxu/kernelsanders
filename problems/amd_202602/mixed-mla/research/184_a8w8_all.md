# Attempt 184: a8w8 for ALL assembly (qSeqLen=1 optimized kernel)

## CRITICAL DISCOVERY
From mla_asm.csv:
- a16w8 persistent: qSeqLen=4 only (m16x4_n16x1) — wastes 3/4 compute for decode!
- a8w8 persistent: qSeqLen=1 dedicated (qseqlen1_gqaratio16) — perfectly matched!

## Expected Impact
- bs=64/kv=1024: 50.6µs → ~43µs (qSeqLen=1 kernel, -Q quant +3µs)
- bs=256/kv=1024: 113µs → ~106µs
- kv>1024: unchanged (already using a8w8 with qSeqLen=1)
